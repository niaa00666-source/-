[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Приложение одностраничное: любой путь отдаёт index.html, иначе прямой заход
# по адресу вернёт 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
