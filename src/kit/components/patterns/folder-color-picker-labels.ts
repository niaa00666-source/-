import type { SupportColorRole } from "@/lib/support-colors"

/**
 * Контракт всех пользовательских подписей паттерна `FolderColorPicker`.
 *
 * Язык принадлежит МЕСТУ: у компонента нет подписей-дефолтов, весь текст
 * приходит снаружи через обязательный проп `labels` — как у `ShareAccessLabels`.
 * RU-набор для макетов — `ruFolderColorPickerLabels`; он несёт строки места, а
 * не дефолт компонента, и передаётся явно. Опциональным `labels` здесь быть не
 * может: тогда потребитель, забывший проп, молча отгрузил бы русский текст, и
 * тип бы об этом не сказал.
 *
 * Форма СОВПАДАЕТ с `FileTableLabels["folderColors"]` из `@scrambled/screens`
 * намеренно и по одной причине: продукт, у которого таблица файлов уже говорит
 * своим бандлом, передаёт сюда `labels.folderColors` целиком и не собирает
 * второй словарь названий цветов. Меняя форму, поменяйте её в обоих местах —
 * иначе у названий цветов появится второй источник правды.
 */
export interface FolderColorPickerLabels {
  /**
   * Заголовок группы в меню И accessible name самой радиогруппы («Цвет папки»).
   *
   * Одна строка на два места сознательно: у радиогруппы из цветных кружков нет
   * видимой подписи, кроме заголовка группы, и второе название расходилось бы с
   * первым.
   */
  groupLabel: string
  /**
   * Название каждой роли палитры — `aria-label` и `title` кружка.
   *
   * Ключ — роль (`support-amber`), а не человеческое имя: подпись локализуема,
   * роль — нет, и на роли ключуется и тонировка папки, и то, что уходит на
   * бекенд.
   */
  names: Record<SupportColorRole, string>
}

/**
 * RU-фикстура подписей палитры цвета папки — набор МЕСТА для макетов.
 *
 * Названия цветов — бытовые, а не имена ролей: пользователь выбирает «Красный»,
 * даже когда роль зовётся `support-tomato`.
 */
export const ruFolderColorPickerLabels: FolderColorPickerLabels = {
  groupLabel: "Цвет папки",
  names: {
    "support-rose": "Розовый",
    "support-tomato": "Красный",
    "support-amber": "Янтарный",
    "support-lime": "Лаймовый",
    "support-green": "Зелёный",
    "support-teal": "Бирюзовый",
    "support-azure": "Голубой",
    "support-indigo": "Индиго",
    "support-purple": "Фиолетовый",
    "support-brown": "Коричневый",
    "support-gray": "Серый",
    "support-slate": "Графитовый",
  },
}
